<h1>All Pets<h1>
 <% @pets.each do |pet|  %>
    <p><%=  pet.name  %><p> 
    <%= image_tag(pet.image) %>
    <p><%=  pet.approximate_age  %><p> 
    <p><%=  pet.sex  %><p> 
    <p><%=  pet.shelter.name  %><p> 
 <% end %>
 <%= link_to  "Create New pet", new_pet_path %>